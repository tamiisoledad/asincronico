<%- include('./partials/head', { title : "listado de Generos" }) %> 
<body>
    <%- include('./partials/header') %> 
    <main>
        <h1>LISTADO DE GENEROS</h1>
        <ul>
            <% for( let i = 0; i < genres.length; i++ ) { %>
            <li>
                <a href="/genres/detail/<%= genres[i].id %>"><%= genres[i].name %></a>
            </li>
            <% } %>
        
        </ul>
    </main>
<%- include('./partials/bodyEnd.ejs') %> 
